version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "1337:1337"
    volumes:
      - type: bind
        source: ./uploads
        target: /data/ditteograsmus
        is_directory: true
